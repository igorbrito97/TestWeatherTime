using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class SceneController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // string scene = PlayerPrefabs.GetString("scene");
        // string weather = PlayerPrefabs.GetString("weather");
        // string time = PlayerPrefabs.GetString("time");
        // Debug.Log("VAMOS VER " + weather + " " + scene + " " + time);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
